apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Apps layer for the mobile cluster.
# Add application manifests (drone-demo, IoT simulation, Grafana dashboard
# ConfigMap, kube-proxy watchdog, etc.) as resources here once infrastructure
# is validated on the mobile cluster.
resources: []
